# Spring Application Settings
server.port=8080

# MySQL Database Configuration
# NOTE: Replace 'your_username' and 'your_password' with your actual MySQL credentials
spring.datasource.url=jdbc:mysql://localhost:3306/money_manager?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&defaultRowPrefetch=1
spring.datasource.username=root
spring.datasource.password=Karan@8285
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Properties
# 'update' tells Hibernate to create the 'transaction' table automatically based on the Entity model.
spring.jpa.hibernate.ddl-auto=update
# To see the SQL queries generated by Hibernate (useful for debugging)
spring.jpa.show-sql=true
